extends layout

block content
	ul.breadcrumb
		li
			a(href='/catalog') Home
			i.icon-angle-right
		li
			a(href='/catalog/tags') Tags
	h1= title
	ul
		each tag in tag_list
			li 
				a(href=tag.url) #{tag.name}
		else
			li There are no tags.